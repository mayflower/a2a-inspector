# Git
.git
.gitignore

# IDE / Editor files
.vscode/
.idea/
*.swp
*~

# Python specific
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/

# Node / Frontend specific
# Must be **/node_modules: a bare `node_modules/` only matches the repo root,
# so a developer's frontend/node_modules would be copied into the image and
# shadow the one npm ci installs -- native binaries such as esbuild are then
# built for the wrong platform and `npm run build` dies with "Exec format
# error". CI never sees this because a fresh checkout has no node_modules.
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
frontend/dist/
frontend/build/
frontend/out/

# Docker specific
Dockerfile
.dockerignore

# Logs and temporary files
*.log
tmp/
temp/

# Environment files
.env
.env.*

# Local database files
*.sqlite
*.db

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/

# Dependency directories
jspm_packages/

# Build outputs
build/
dist/

# Cache
.npm
.eslintcache

# Environment variables
.env.local
.env.development.local
.env.test.local
.env.production.local
