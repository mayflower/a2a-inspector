{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "a2a-inspector.serviceAccountName" . }}
  labels:
    {{- include "a2a-inspector.labels" . | nindent 4 }}
# The inspector never talks to the Kubernetes API.
automountServiceAccountToken: false
{{- end }}
